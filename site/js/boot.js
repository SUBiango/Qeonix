/* Loaded synchronously in <head>: stamps the JS flag before first paint so the
   reveal system arms itself. Without it every .reveal stays plainly visible. */
document.documentElement.className += " js";
